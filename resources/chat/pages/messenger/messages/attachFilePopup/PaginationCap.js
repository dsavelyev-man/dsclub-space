import React from "react";
import classname from "classname"

const classNames = {
  container: "flex justify-center items-center text-slate-100 w-full h-10",
}

const PaginationCap = () => {

  return <div className={classNames.container}>
  </div>
}

export default PaginationCap
