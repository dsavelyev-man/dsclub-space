import React from "react";

const classNames = {
  wrapper: "w-full bg-slate-800 drop-shadow-xl ml-2 flex justify-center text-slate-600 items-center rounded-xl h-full"
}

const MessagesCap = () => {
  return <div className={classNames.wrapper}>
    <h1>Select Chat</h1>
  </div>
}

export default MessagesCap
